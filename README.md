# Recursive-Flatten-List-Program-in-python
Python program that flattens deeply nested lists using recursion. The function traverses nested structures, detects sublists using isinstance(), and extracts all values into a single linear list. Built to explore recursion, nested data handling, and Python function behavior.
